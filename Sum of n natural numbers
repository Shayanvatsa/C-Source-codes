#include<stdio.h>
void main()
{
	int i,sum=0,n;
	printf("Enter the last number= ");
	scanf("%d",&n);
	for(i=1;i<=n;i++)
	{
		if(i!=n)
			printf("%d + ",i);
		else
			printf("%d ",i);
		sum=sum+i;
	}
	printf("= %d",sum);
}
